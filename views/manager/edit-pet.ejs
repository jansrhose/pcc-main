<!DOCTYPE html>
<html>
<head>
  <title>Manager Dashboard</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: "Bruno Ace", cursive;
      background-image: linear-gradient(90deg, #DAA06D , #EADDCA);
    }

    header{
      background-color: #333;
      font-family: "Bruno Ace", cursive;
      justify-content: flex-start; 
      text-align: left;
      align-content: flex-start;
    }

    nav {
      padding: 0 20px;
      height: 60px;
    }

    nav ul {
      list-style: none;
      margin: 0;
      padding: 0;
      display: flex;
    }

    nav ul li {
      margin-right: 20px;
    }

    nav ul li a {
      color: gray;
      text-decoration: none;
      position: relative;
      transition: color 0.3s, opacity 0.3s;
    }

    nav ul li a:before {
      content: "";
      position: absolute;
      width: 100%;
      height: 2px;
      background-color: #DAA06D;
      bottom: -5px;
      left: 0;
      visibility: hidden;
      transform: scaleX(0);
      transition: all 0.3s ease-in-out;
    }

    nav ul li a:hover {
      color: #DAA06D;
      opacity: 0.8;
    }

    nav ul li a:hover:before {
      visibility: visible;
      transform: scaleX(1);
    }

    h1 {
      font-size: 36px;
      color: #DAA06D;
      margin-bottom: 40px;
      padding: 1rem;
      transform: translate(10%, 10%);
      display: inline-block;
      background: #EADDCA;
      font-family: "Bruno Ace", cursive;
      border-radius: 5rem;
      box-shadow: 0 0 0.5rem 0.5rem rgba(234, 221, 202, 0.5);
      font-size: 2.5rem;
      width: fit-content; /* Added width property */
      margin-top: 15px;
    }

    form {
      margin-top: 2rem;
    }

    form div {
      margin-bottom: 1rem;
    }

    form label {
      display: inline-block;
      width: 100px;
      font-weight: bold;
      font-family: "Bruno Ace", cursive;
    }

    form input[type="text"],
    form input[type="number"],
    form input[type="email"],
    form input[type="password"] {
      width: 300px;
      padding: 0.5rem;
      font-family: Arial, sans-serif;
      border-radius: 4px;
      border: 1px solid #ccc;
    }

    form button {
      padding: 0.5rem 1rem;
      font-size: 16px;
      color: #DAA06D;
      background-color: #EADDCA;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      transition: background-color 0.3s;
      font-family: "Bruno Ace", cursive;
    }

    form button:hover {
      background-color: #b68051;
    }

    h2{
      font-family: "Bruno Ace", cursive;
    }
  </style>
  <link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.0/dist/css/bootstrap.min.css">
</head>
<body>
  <header>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <a class="navbar-brand" href="/manager/dashboard">Manager Dashboard</a>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav ml-auto"> <!-- Use "ml-auto" class to align items to the left -->
            <li class="nav-item">
              <a href="/manager/dashboard">Home</a>
            </li>
            <li class="nav-item">
              <a href="/manager/dashboard">Pet List</a>
            </li>
            <li class="nav-item">
              <a href="/manager/logout">Logout</a>
            </li>
          </ul>
        </div>
    </nav>
  </header>
  <main>
    <div class="container"> 
      <h1>Edit Pet</h1>
      <form method="POST" action="/manager/edit-pet">
        <div class="row">
          <div class=" form-group col-sm-4 mb-3">
            <label for="managerId">Manager ID</label>
            <input type="text" id="managerId" name="managerId" value="<%= managerId %>" readonly required>
          </div>
        </div>
        <div class="row">
          <h2>Pet's Information</h2>
          <div class=" form-group col-sm-4 mb-3">
            <label for="petName">Pet Name</label>
            <input type="text" id="petName" name="petName" value="<%= pet.petName %>" required>
          </div>
          <div class=" form-group col-sm-4 mb-3">
            <label for="breed">Breed</label>
            <input type="text" id="breed" name="breed" value="<%= pet.breed %>" required>
          </div>
          <div class=" form-group col-sm-4 mb-3">
            <label for="age">Age</label>
            <input type="number" id="age" name="age" value="<%= pet.age %>" required>
          </div>
        </div>
        <div class="row">
          <div class=" form-group col-sm-4 mb-3">
            <label for="weight">Weight</label>
            <input type="number" id="weight" name="weight" value="<%= pet.weight %>">
          </div>
          <div class=" form-group col-sm-4 mb-3">
            <label for="gender">Gender</label>
            <input type="text" id="gender" name="gender" value="<%= pet.gender %>" required>
          </div>
          <div class=" form-group col-sm-4 mb-3">
            <label for="service">Service</label>
            <input type="text" id="service" name="service" value="<%= pet.service %>" required>
          </div>
        </div>
        <div class="row">
          <h2>Owner's Information</h2>
          <div>
            <label for="owner">Owner</label>
            <input type="text" id="owner" name="owner" value="<%= pet.owner %>" required>
          </div>
          <div>
            <label for="contact">Contact</label>
            <input type="number" id="contact" name="contact" min="11" max="11" value="<%= pet.contact %>" required>
          </div>
          <div>
            <label for="email">Email</label>
            <input type="email" id="email" name="email" value="<%= pet.email %>" required>
          </div>
        </div>
        <div>
          <input type="hidden" name="petId" value="<%= pet.id %>">
          <button type="submit">Edit Pet</button>
        </div>
      </form>
    </div>
  </main>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.0/dist/js/bootstrap.bundle.min.js"></script> 
</body>
</html>